// Fill this in with YOUR Firebase project's config (see README.md — Step 1).
// These values are meant to be public/client-side; safety is enforced by Firestore rules, not secrecy.
export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID",
};

// Flatmates, in a fixed rotation order. Edit names/order here.
export const NAMES = ["H", "J", "S", "V", "Y"];
